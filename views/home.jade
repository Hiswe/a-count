extends _layout.jade

block content
  h1 ConCompte
  table.table-pres(cellspacing=0)
    thead
      tr
        th id
        th title
        th customer
    tbody
      each quotation in quotations
        tr
          td= quotation._id
          td= quotation.title
          td= quotation.customer
