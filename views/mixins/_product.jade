mixin _product(index, opts)
  - opts = opts || {};

  tr.js-product(id="product-#{index}")
    td: input.js-product-descriptions(
      type="text"
      name="products[#{index}][description]"
      value=opts.description
    )
    td: input.js-product-quantity(
      type="number"
      name="products[#{index}][quantity]"
      value=opts.quantity
    )
    td: input.js-product-price(
      type="number"
      name="products[#{index}][price]"
      value=opts.price
    )
    //- TODO put taxes default in conf
    td: input.js-product-tax(
      type="number"
      name="products[#{index}][taxes]"
      value=opts.taxes
    )
    td.total
      | €&nbsp;
      span.js-product-total 350

