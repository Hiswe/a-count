doctype html
html&attributes( helmet.htmlAttributes.toComponent() )
  head
    | !{ helmet.title.toString() }
    | !{ helmet.meta.toString() }
    | !{ helmet.link.toString() }
  body
    include svg-icons.svg

    #react-main-mount
      | !{dom}

    script.
      window.__CONFIG__ = !{config}
      window.__INITIAL_STATE__ = !{initialState}
    script(src="/vendor.concompte.js")
    script(src="/concompte.js")

