extends ./_base-layout.jade

block content
  #react-main-mount
    | !{dom}
  if initialState
    script.
      window.__INITIAL_STATE__ = !{JSON.stringify(initialState)}

  script(src="/concompte-lib.js")
  script(src="/concompte.js")

