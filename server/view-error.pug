doctype html
main(role="main")
  h1 [SERVER] error
  pre= reason
  pre= description
  hr
  if error
    pre= error
  if stacktrace
    if Array.isArray(stacktrace)
      pre= stacktrace.join('\n')
    else
      pre= stacktrace
